module main (absLet, classifyDo)

import root-cpp

absLet :: Int -> Int
absLet x =
  let result ? x >= 0 = x
             : neg x
  in result

classifyDo :: Int -> <IO> Str
classifyDo x = do
  let label ? x > 100 = "big"
            ? x > 10 = "medium"
            : "small"
  label
