-- @streamLayout requires a list-shaped IFile ([a]); IFile Int must be rejected.
module main (bad)

import root

bad :: Str -> <IO> [(U64, U64, U64)]
bad path = do
  Ok f <- @open path :: <IO> (Try Str (IFile Int))
  Ok _tailv100 <- @streamLayout f
  _tailv100
