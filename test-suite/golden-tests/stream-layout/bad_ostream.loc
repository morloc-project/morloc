-- @streamLayout requires an IFile receiver; an OStream must be rejected.
module main (bad)

import root

bad :: Str -> <IO> [(U64, U64, U64)]
bad path = do
  Ok o <- @open path :: <IO> (Try Str (OStream Int))
  Ok _tailv101 <- @streamLayout o
  _tailv101
