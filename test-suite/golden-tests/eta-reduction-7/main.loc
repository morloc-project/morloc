import pybase (map, id, add)

bar :: Real -> [Real] -> [Real]
bar Py :: "float" -> ["float"] -> ["float"]
bar x = map id

-- -- substituting for this bar works ...
-- bar x = map (add 1.0)

export bar
