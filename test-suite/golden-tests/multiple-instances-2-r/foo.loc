module main (rms)

import math (sqrt, mean)
import root-r (Functor, Integral)

source r from "rms.R"
  ( "rms1" as rms
  )

square x = mul x x
rms xs = sqrt (mean (map square xs))

rms :: [Real] -> Real
