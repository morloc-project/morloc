module main (rms)

import math-r (sqrt)
import root-r (Functor, Integral)

source r from "rms.R"
  ( "rms1" as rms
  )

mean :: [Real] -> Real
mean xs = fold1 (+) xs / length xs

square x = x * x
rms xs = sqrt (mean (map square xs))

rms :: [Real] -> Real
