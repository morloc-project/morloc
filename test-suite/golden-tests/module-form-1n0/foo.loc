module main (sub)

-- 1n0 - souce 1, import n

import pybase (sub)
import cppbase (sub)
import rbase (sub)
import conventions (Real)

source py from "foo.py" ("sub_loc" as sub)
sub :: Real -> Real -> Real
