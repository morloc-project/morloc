-- A C++ Matrix crossing to the nexus. Matrix is
-- ((Int, Int), Vector (m * n) a), so the JSON is the shape pair
-- followed by the flat row-major data, not nested rows.

module main (makeMatrix)

import root
import root-cpp
import tensor
import tensor-cpp

source Cpp from "src.hpp" ("makeMatrix")

makeMatrix :: Matrix 3 4 Int
