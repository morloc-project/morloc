import math (mul, mean, sqrt, sum);
import math (sqrt)
import cppbase (map);

export square;
export rms;
export sqrtsum;
export nested_map;

square x = mul x x;
rms xs = sqrt (sum (map square xs));
sqrtsum xs = sum (map sqrt xs);
nested_map xs = sum (map square (map sqrt xs));
