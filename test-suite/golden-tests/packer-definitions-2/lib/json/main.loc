module lib.json (JsonObj, Str)

source Py from "json.py"
  ( "packJsonObj" as packJsonObj
  , "unpackJsonObj" as unpackJsonObj
  )

type Py Str = "str"
type Py JsonObj = "dict"

packJsonObj   :: pack   => Str -> JsonObj
unpackJsonObj :: unpack => JsonObj -> Str
