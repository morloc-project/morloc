module main (shouldFail)

import root-py

type Email = Str

class Greet a where
  greet :: a -> Str

-- R1 violation: instance on transparent alias.
instance Greet Email where
  source Py from "helpers.py" ("greet_email" as greet)

shouldFail = greet ("user@example.com" :: Email)
