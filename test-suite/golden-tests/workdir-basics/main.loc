module main (foo, bar)

import root-py

sum :: [Real] -> Real
sum = fold (+) 0.0

-- Labeled: every call should materialize $MORLOC_RUN_BASE/<id>/a/log
foo :: [Real] -> Real
foo = a:sum

-- Unlabeled: no log emission, so the run dir must NEVER be materialized
bar :: [Real] -> Real
bar = sum
