import pybase ()

source py from "more.py" ("dict" as Map, "lookup");

export foo

Map :: [(key,val)] -> Map key val;
Map py :: [(key,val)] -> "dict" key val;

lookup :: key -> Map key val -> val;
lookup py :: key -> Map key val -> val;

foo :: Str -> Map Str Int -> Int;
foo k xs = lookup k xs
