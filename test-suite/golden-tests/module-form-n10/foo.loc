module main (sub)

-- n10 - source n, import n

import types (Real)
import cppbase (sub)
import pybase (sub)
import rbase (sub)

source py from "foo.py" ("sub_loc" as sub)
source r from "foo.R" ("sub_loc" as sub)

sub :: Real -> Real -> Real
