module main (foo)

type Py => Int = "int" 
type Py => Tuple2 a b = "tuple" a b

source Py from "foo.py" ("add") 
add :: Int -> Int -> Int

toSnd a b :: (a -> b) -> a -> (a, b)
toSnd f x = (x, f x)

foo x = toSnd (add 1) x
