module main (foo)

import root

-- `form: bytes-only` on a fixed-width-scalar array reads the file as
-- packed raw bytes without packet sniffing.
foo ::
  --' form: bytes-only
  [F64] ->
  Int
foo x = 0
