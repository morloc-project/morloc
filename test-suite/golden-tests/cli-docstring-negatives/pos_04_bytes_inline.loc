module main (foo)

import root

-- `[U8]` with `source: inline + form: bytes` is allowed: the argv
-- string's bytes (after escape-sequence interpretation) become the
-- byte buffer.
foo ::
  --' source: inline
  --' form: bytes
  [U8] ->
  Int
foo x = 0
