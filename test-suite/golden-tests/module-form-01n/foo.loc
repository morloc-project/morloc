-- 01n - single import and multiple declarations

import math (sum);
import pybase (fold, add, mul);
import cppbase (fold, add, mul);
import rbase (fold, add, mul);

export sum;

sum xs = fold add 0 xs;
sum xs = mul 1 (fold add 0 xs);
