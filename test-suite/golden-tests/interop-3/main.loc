import pybase (map, add)
import rbase (fold)
export foo

sum R :: ["numeric"] -> "numeric" -> "numeric"
sum xs b = fold add b xs

foo xs = sum (map (add 1.0) xs) 0.0

-- foo xs = fold add b (map (add 1.0) xs) 0.0
--          R    Py     Py   Py
