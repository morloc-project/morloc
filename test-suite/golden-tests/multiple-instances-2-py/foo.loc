module main (rms)

import math (sqrt, mean)
import root-py (Functor, Integral)

source py from "rms.py"
  ( "rms1" as rms
  )

square x = x * x
rms xs = sqrt (mean (map square xs))

rms :: [Real] -> Real
