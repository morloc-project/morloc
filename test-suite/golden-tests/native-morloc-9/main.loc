module main (foo)

test1 :: Str -> Str -> Str
test1 x y = "#{x}+#{y}=#{x}#{y}"

far = (123, "zanzibar")

foo x =
  [ test1 x "pickle"
  , "somewhere in #{.1 far}"
  , "#{x}"
  , "#{x}#{x}"
  , "<#{x}>"
  , "#{x}>"
  , "<#{x}"
  , "#{x}.#{x}"
  ]
