module m (foo)

import conventions (Str, Int, Real, Bool, Tuple2)
source Cpp from "foo.hpp" ("f", "g")
source Py from "foo.py" ("h")

f :: (a -> b) -> a -> b
g :: Str -> Int
h :: (Str, Int) -> Str

foo x y = f g (h (x, y))
