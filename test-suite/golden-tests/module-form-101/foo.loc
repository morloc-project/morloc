module main (foo)

-- 101 - source 1, declare 1

import types (Real)
import base-py  (sub, add, mul)
import base-cpp (sub, add, mul)
import base-r   (sub, add, mul)

source py from "foo.py" ("foo_loc" as foo)

foo x = add 1.0 x
