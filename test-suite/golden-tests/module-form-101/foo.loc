module main (foo)

-- 101 - source 1, declare 1

import types (Real)
import pybase  (sub, add, mul)
import cppbase (sub, add, mul)
import rbase   (sub, add, mul)

source py from "foo.py" ("foo_loc" as foo)

foo x = add 1.0 x
