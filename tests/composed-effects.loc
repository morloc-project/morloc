@comment

This code is equivalent to

man ls | grep -Po "[a-z]+ed" | sort | uniq

@source sh
store () {
    cat $1 > $2
}

@path
A :: uniq . sort . grep . man

@arg
grep :: -Po "[a-z]+ed"
man :: ls

@lang
uniq,sort,grep,man :: sh

@effect
sort :: store . head `sort.txt`
