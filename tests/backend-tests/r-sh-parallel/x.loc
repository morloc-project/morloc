@type
paste :: Int -> Num -> String
mean :: [Num] -> Num
rnorm :: Int -> Num -> Num -> [Num]
seq :: Int -> Int -> [Int]

@path
# OK, so this isn't actually parallel ...
# But just replace `map` with somthing that runs things in the background,
# and you are pure gold.
map . &( paste . $1 (mean . rnorm . 10 0 1) ) (seq . 1 3)

@lang
* :: R
map :: sh

@0
paste :: set.seed . 42

@include
core/type
core/control
