@source sh

map () {
    while read arg
    do
        $1 "$arg"
    done < "$2"
}

@path
map . &(cat . cut . wc . grep . $1 ) ls . `*loc` `*py`

@lang
* :: sh

@arg
grep :: "a" -o
wc :: -l
cut :: --delimiter=" " --fields=1
