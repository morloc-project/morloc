@source R

is_positive <- function(x){ all(x >= 0) }
print_na <- function(...){ "na" }

@path
sqrt . max . seq

@arg
seq := from=-1 to=-10

@lang
* :: R

@check
sqrt := is_positive . <max>

@fail
sqrt :: print_na
