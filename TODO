# Infrastructure

Use the right tool for the right job

 [ ] Move from `parsec` to `megaparsec`
 [ ] Move to `Text` as my sole string representation
 [ ] Use a builder combinator (e.g. wl-pprint-text) for code generation
 [ ] Replace ad hoc RDF querying with SPARQL (via hsparql for now)
 [ ] Add graph names and a reliable import system
 [ ] Test for all v0.13.1 functionallity
