import math (add, mul)
import optimization (optimize1D)

export quadraticEq
export optimalQuadratic

quadraticEq :: Num -> Num -> Num -> Num -> Num;
quadraticEq a b c x = add (mul a (mul x x)) (add (mul b x) c);

optimalQuadratic a b c lbnd rbnd = optimize1D (quadraticEq a b c) lbnd rbnd;
