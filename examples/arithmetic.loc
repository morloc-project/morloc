import rbase (map, mul);

source "R" ("sqrt", "sum");

export rms;

square :: Num -> Num;
square x = mul x x;

sqrt :: Num -> Num;
sqrt R :: "numeric" -> "numeric";

sum :: [Num] -> Num;
sum R :: "numeric" -> "numeric";

rms :: [Num] -> Num;
rms xs = sqrt (sum (map square xs));
