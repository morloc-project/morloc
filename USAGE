morloc version 0.17.2-9000

Usage:
  morloc make [--expression] [--endpoint=<sparql-endpoint>] [--config=<config>] [--vanilla] <script>
  morloc rdf [--triple] [--expression] [--config=<config>] [--vanilla] <script>
  morloc install [--github] [--config=<config>] [--vanilla] <name>

Options:
  -t, --triple       print RDF graph in triple format, rather than the turtle
  -e, --expression   read script as string rather than file
  --vanilla          ignore local configuration files
