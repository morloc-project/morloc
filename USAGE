morloc version 0.16.2-9000

Usage:
  morloc make [--expression] [--endpoint=<sparql-endpoint>] [--config=<config>] <script>
  morloc rdf [--triple] [--expression] [--config=<config>] <script>
  morloc install [--github] [--config=<config>] <name>
  morloc init [--config=<config>] <package-name>
  morloc check [--config=<config>] [<package-name>]

Options:
  -t, --triple       print RDF graph in triple format, rather than the turtle
  -e, --expression   read script as string rather than file
